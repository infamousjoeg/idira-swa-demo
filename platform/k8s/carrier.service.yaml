apiVersion: v1
kind: Service
metadata:
  name: carrier
  namespace: swa-demo
spec:
  selector: {app: carrier}
  ports:
    - port: 8443
      targetPort: 8443
      name: https
    - port: 8444
      targetPort: 8444
      name: trace
